# Ignore all folders inside the 'data' directory, except StudentLife dataset surveys answers
data/*/
!data/StudentLife/dataset/EMA/response/**

# Ignore all files inside the 'results' directory
results/*
